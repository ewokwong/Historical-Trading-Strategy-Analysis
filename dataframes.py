# File containing dataframes for FillPortfolio method such that doesn't have to keep calling everytime
import functions 

shares_to_test = ['GOOGL', 'AMZN', 'FB', 'MSFT', 'NFLX']
data_dict = functions.getTestDataframes(shares_to_test)